@using WebApp_GozenBv.Constants;
@model WebApp_GozenBv.ViewModels.StatusIconViewModel;

<td class="d-flex flex-row justify-content-start">
    @if (Model.Status == StockLogStatusConst.Created)
    {
        <img class="p-0 col-4" src="~/resources/icons/check-svgrepo-com.svg" />
    }

    @if (Model.Status == StockLogStatusConst.DamagedAwaitingAction)
    {
        <img class="p-0 col-4" src="~/resources/icons/red-exclamation-mark-svgrepo-com.svg" />
    }

    @if (Model.Status == StockLogStatusConst.Returned)
    {
        <img class="p-0 col-4" src="~/resources/icons/return-svgrepo-com.svg" />
        if (Model.IsDamaged)
        {
            <img class="p-0 col-4" src="~/resources/icons/red-exclamation-mark-svgrepo-com.svg" />
        }
    }
</td>